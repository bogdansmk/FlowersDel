import colors from './colors';

const fonts = {
  heading: {
    fontSize: 25,
    textAlign: 'center',
    color: colors.headingColor,
    lineHeight: 27,
  },
  smallHeading: {
    fontSize: 20,
    textAlign: 'center',
    color: colors.headingColor,
    lineHeight: 22,
  },
  text: {
    fontSize: 15,
    color: colors.textColor,
  },
  largeText: {
    fontSize: 18,
    color: colors.textColor,
  },
};

export default fonts;
